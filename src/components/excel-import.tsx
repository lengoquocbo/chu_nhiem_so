"use client";
import { useActionState } from "react";
import { confirmStudentImport, previewStudentExcel } from "@/app/hoc-sinh/actions";
export function ExcelImport(){const[state,action,pending]=useActionState(previewStudentExcel,null);const valid=state?.rows?.length&&state.rows.every(r=>r.errors.length===0);return <div className="space-y-5"><form action={action} className="card p-5"><label className="block"><span className="mb-2 block font-semibold">Chọn file Excel (.xlsx) *</span><input type="file" name="file" accept=".xlsx,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" required className="input pt-2"/></label><p className="mt-2 text-sm text-slate-500">Tối đa 5 MB. Hãy dùng file mẫu để tránh sai cột.</p><button className="btn btn-primary mt-4" disabled={pending}>{pending?"Đang đọc file...":"Xem trước dữ liệu"}</button>{state?.error&&<p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 text-red-700">{state.error}</p>}</form>{state?.rows&&<section className="card p-5"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-bold">Xem trước {state.rows.length} dòng</h2><p className="text-sm text-slate-500">Kiểm tra kỹ trước khi xác nhận ghi vào cơ sở dữ liệu.</p></div><span className={`badge ${valid?"bg-green-50 text-green-700":"bg-red-50 text-red-700"}`}>{valid?"Dữ liệu hợp lệ":`${state.rows.filter(r=>r.errors.length).length} dòng có lỗi`}</span></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Dòng</th><th>Mã</th><th>Họ tên</th><th>Ngày sinh</th><th>Tổ</th><th>Phụ huynh</th><th>Kiểm tra</th></tr></thead><tbody>{state.rows.map(r=><tr key={r.row}><td>{r.row}</td><td>{r.code}</td><td className="font-semibold">{r.fullName}</td><td>{r.birthDate||"—"}</td><td>{r.team||"—"}</td><td>{r.guardianName||"—"}</td><td>{r.errors.length?<span className="text-red-700">{r.errors.join("; ")}</span>:<span className="text-green-700">Hợp lệ</span>}</td></tr>)}</tbody></table></div>{valid&&<form action={confirmStudentImport} className="mt-4"><input type="hidden" name="rows" value={JSON.stringify(state.rows)}/><button className="btn btn-primary">Xác nhận nhập {state.rows.length} học sinh</button></form>}</section>}</div>}
