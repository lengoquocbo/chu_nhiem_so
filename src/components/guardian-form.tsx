"use client";
import { useActionState } from "react";
import { addGuardian } from "@/app/hoc-sinh/actions";
export function GuardianForm({ studentId }: { studentId: string }) { const [state, action, pending] = useActionState(addGuardian, null); return <form action={action} className="grid gap-3 md:grid-cols-2"><input type="hidden" name="studentId" value={studentId}/><label><span className="mb-1 block text-sm font-semibold">Họ tên *</span><input name="name" className="input" required/></label><label><span className="mb-1 block text-sm font-semibold">Quan hệ *</span><input name="relation" className="input" required placeholder="Bố, mẹ..."/></label><label><span className="mb-1 block text-sm font-semibold">Số điện thoại</span><input name="phone" className="input" inputMode="tel" placeholder="0901234567"/></label><label><span className="mb-1 block text-sm font-semibold">Email</span><input name="email" className="input" type="email"/></label><label className="flex items-center gap-2"><input type="checkbox" name="isPrimary" value="true"/> Người liên hệ chính</label><div className="flex items-center justify-end"><button className="btn btn-primary" disabled={pending}>{pending?"Đang lưu...":"Thêm người giám hộ"}</button></div>{state?.error&&<p role="alert" className="md:col-span-2 rounded-xl bg-red-50 p-3 text-red-700">{state.error}</p>}{state?.ok&&<p role="status" className="md:col-span-2 rounded-xl bg-green-50 p-3 text-green-700">Đã thêm người giám hộ.</p>}</form> }
