"use client";
import { useActionState } from "react";
import { createManagedAccount, resetAccountPassword } from "./actions";
export function CreateAccountForm({ schools, classes, teams }: { schools: { id: string; name: string }[]; classes: { id: string; name: string; schoolId: string }[]; teams: { id: string; name: string; classId: string }[] }) { const [state, action, pending] = useActionState(createManagedAccount, null); return <form action={action} className="card grid gap-4 p-5 md:grid-cols-3"><label><span className="label">Họ và tên</span><input className="input" name="name" required /></label><label><span className="label">Tên đăng nhập hoặc email</span><input className="input" name="identifier" required /></label><label><span className="label">Mật khẩu tạm</span><input className="input" name="password" type="password" minLength={10} required /></label><label><span className="label">Vai trò</span><select className="input" name="role">{[["TEACHER","Giáo viên"],["CLASS_MONITOR","Lớp trưởng"],["ACADEMIC_VICE_MONITOR","Lớp phó học tập"],["DISCIPLINE_VICE_MONITOR","Lớp phó kỷ luật"],["TEAM_LEADER","Tổ trưởng"],["STUDENT","Học sinh"],["GUARDIAN","Phụ huynh"]].map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label><span className="label">Trường</span><select className="input" name="schoolId">{schools.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label><span className="label">Lớp</span><select className="input" name="classId"><option value="">Chưa gán</option>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label><span className="label">Tổ</span><select className="input" name="teamId"><option value="">Không áp dụng</option>{teams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label><span className="label">Ngày bắt đầu</span><input className="input" name="startDate" type="date" /></label><label><span className="label">Ngày kết thúc</span><input className="input" name="endDate" type="date" /></label>{state?.error&&<p role="alert" className="md:col-span-3 text-red-700">{state.error}</p>}{state?.ok&&<p role="status" className="md:col-span-3 text-emerald-700">{state.message}</p>}<button className="btn btn-primary md:col-span-3" disabled={pending}>Tạo tài khoản</button></form>; }
export function ResetPasswordForm({ id }: { id: string }) { const [state, action, pending] = useActionState(resetAccountPassword, null); return <form action={action} className="flex gap-2"><input type="hidden" name="id" value={id}/><input className="input max-w-52" name="password" type="password" placeholder="Mật khẩu tạm mới" minLength={10} required/><button className="btn btn-soft" disabled={pending}>Đặt lại</button>{state?.error&&<span className="text-xs text-red-700">{state.error}</span>}</form>; }