import { requireUser } from "@/lib/auth";
import { Shell } from "@/components/shell";
import { getLicenseRuntime } from "@/lib/license/runtime";
import { getRemainingDays } from "@/lib/license/core";
import { LicenseImportForm } from "./license-import-form";
import { InstallationCode } from "./installation-code";
const labels: Record<string, string> = { DEVELOPMENT_BYPASS: "Chế độ phát triển — không yêu cầu license", MISSING: "Chưa có license", INVALID_SIGNATURE: "Chữ ký không hợp lệ", INVALID_PRODUCT: "License không đúng sản phẩm", WRONG_INSTALLATION: "License không đúng máy", SCHEDULED: "Chưa đến ngày sử dụng", ACTIVE: "Đang hoạt động", GRACE: "Trong thời gian gia hạn", READ_ONLY_EXPIRED: "Đã hết hạn — chỉ đọc" };
export default async function Page() { const user = await requireUser(), state = await getLicenseRuntime(), p = state.payload; return <Shell user={user}><h1 className="text-3xl font-extrabold">Quản lý bản quyền</h1><p className="mt-2 text-slate-500">License ngoại tuyến có chữ ký số Ed25519. Dữ liệu không bị xóa khi license hết hạn.</p><section className="card mt-6 p-5"><h2 className="text-lg font-bold">Mã cài đặt</h2><p className="mb-3 mt-1 text-sm text-slate-500">Mã này chỉ nhận diện bản cài, không thu thập số serial phần cứng.</p><InstallationCode value={state.installationId} /></section><section className="mt-6 grid gap-3 rounded-xl border border-teal-200 bg-teal-50 p-5"><h2 className="font-bold text-teal-900">Cách nhận và nhập license</h2><ol className="list-decimal space-y-2 pl-5 text-sm text-teal-950"><li>Bấm <strong>Sao chép mã</strong> rồi gửi Mã cài đặt cho người bán.</li><li>Người bán tạo và gửi lại một file có đuôi <strong>.license</strong>.</li><li>Ở biểu mẫu bên dưới, bấm <strong>Chọn file</strong>, chọn file được gửi rồi bấm <strong>Nhập license</strong>.</li></ol></section>{!state.writeAllowed && state.status !== "DEVELOPMENT_BYPASS" && <div role="alert" className="mt-6 rounded-xl border border-amber-300 bg-amber-50 p-4 text-amber-900">License chưa cho phép cập nhật dữ liệu. Dữ liệu của thầy/cô vẫn được bảo toàn; vẫn có thể xem, sao lưu, xuất dữ liệu và nhập license mới.</div>}<section className="card mt-6 grid gap-4 p-5 md:grid-cols-2"><div><span className="text-sm text-slate-500">Trạng thái</span><p className="font-bold">{labels[state.status] || state.status}</p></div><div><span className="text-sm text-slate-500">Khách hàng</span><p className="font-bold">{p?.customerName || "Chưa xác định"}</p></div><div><span className="text-sm text-slate-500">Trường</span><p>{p?.schoolName || "—"}</p></div><div><span className="text-sm text-slate-500">Mã license</span><p className="break-all">{p?.licenseId || "—"}</p></div><div><span className="text-sm text-slate-500">Ngày bắt đầu</span><p>{p?.startsAt.slice(0, 10) || "—"}</p></div><div><span className="text-sm text-slate-500">Ngày hết hạn</span><p>{p?.expiresAt || "—"}{p ? ` (${getRemainingDays(p)} ngày còn lại)` : ""}</p></div><div><span className="text-sm text-slate-500">Giới hạn</span><p>{p ? `${p.maxClasses} lớp · ${p.maxStudents} học sinh · ${p.maxChildAccounts} tài khoản con` : "—"}</p></div><div><span className="text-sm text-slate-500">Tính năng</span><p>{p?.enabledFeatures.join(", ") || "—"}</p></div></section><LicenseImportForm /></Shell>; }