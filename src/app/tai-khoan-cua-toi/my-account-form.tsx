"use client";
import { useActionState } from "react";
import { updateMyAccount } from "./actions";
export function MyAccountForm({ user }: { user: { name: string; email: string; username: string | null; mustChangePassword: boolean } }) { const [state, action, pending] = useActionState(updateMyAccount, null); return <form action={action} className="card mt-6 max-w-2xl space-y-4 p-6">{user.mustChangePassword && <p role="alert" className="rounded-xl bg-amber-50 p-3 text-amber-900">Bạn phải đổi mật khẩu tạm trước khi tiếp tục.</p>}<label><span className="label">Họ và tên</span><input className="input" name="name" defaultValue={user.name} required /></label><label><span className="label">Tên đăng nhập hoặc email</span><input className="input" name="identifier" defaultValue={user.username || user.email} required /></label><label><span className="label">Mật khẩu hiện tại</span><input className="input" name="currentPassword" type="password" required /></label><label><span className="label">Mật khẩu mới</span><input className="input" name="newPassword" type="password" minLength={10} required={user.mustChangePassword} /></label><label><span className="label">Nhập lại mật khẩu mới</span><input className="input" name="confirmPassword" type="password" minLength={10} required={user.mustChangePassword} /></label>{state?.error && <p role="alert" className="text-red-700">{state.error}</p>}{state?.ok && <p role="status" className="text-emerald-700">{state.message}</p>}<button className="btn btn-primary" disabled={pending}>{pending ? "Đang lưu…" : "Lưu thay đổi"}</button></form>; }