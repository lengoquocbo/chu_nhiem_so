import{beforeEach,describe,expect,it,vi}from"vitest";import{checkRateLimit,consumeLoginLimits,loginRateKeys,normalizeLoginIdentifier,resetRateLimit,resetLoginEmailLimit}from"./rate-limit";
describe("giới hạn đăng nhập",()=>{beforeEach(()=>{for(const k of ["a","login:email:a@example.com","login:email:b@example.com","login:ip:1.2.3.4"])resetRateLimit(k)});it("chuẩn hóa email",()=>expect(normalizeLoginIdentifier(" A@Example.COM ")).toBe("a@example.com"));it("không dùng khóa chung",()=>expect(loginRateKeys("a@example.com","1.2.3.4").email).toBe("login:email:a@example.com"));it("giới hạn riêng theo email",()=>{consumeLoginLimits("a@example.com","1.2.3.4",1,10);expect(consumeLoginLimits("a@example.com","1.2.3.4",1,10).allowed).toBe(false);expect(consumeLoginLimits("b@example.com","1.2.3.4",1,10).allowed).toBe(true)});it("giới hạn tổng theo IP",()=>{consumeLoginLimits("a@example.com","1.2.3.4",10,1);expect(consumeLoginLimits("b@example.com","1.2.3.4",10,1).allowed).toBe(false)});it("reset email sau thành công",()=>{consumeLoginLimits("a@example.com","1.2.3.4",1,10);resetLoginEmailLimit("a@example.com");expect(consumeLoginLimits("a@example.com","other",1,10).allowed).toBe(true)});it("mở lại sau cửa sổ",()=>{vi.useFakeTimers();checkRateLimit("a",1,1000);vi.advanceTimersByTime(1001);expect(checkRateLimit("a",1,1000).allowed).toBe(true);vi.useRealTimers()})});