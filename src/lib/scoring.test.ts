import{describe,expect,it}from"vitest";import{calculateScore,grade,rankScores}from"./scoring";
describe("scoring engine",()=>{it("cộng, trừ và điều chỉnh điểm",()=>expect(calculateScore(100,[{status:"APPROVED",points:5,type:"BONUS"},{status:"APPROVED",points:-10,type:"PENALTY"}],[2])).toMatchObject({bonus:5,penalty:-10,adjustment:2,finalScore:97}));it("không tính sự kiện bị từ chối hoặc hủy",()=>expect(calculateScore(100,[{status:"REJECTED",points:-10,type:"PENALTY"},{status:"CANCELLED",points:5,type:"BONUS"}]).finalScore).toBe(100));it("giới hạn số lần trong ngày",()=>{const day=new Date("2026-08-18");expect(calculateScore(100,[1,2,3].map(()=>({status:"APPROVED",points:3,type:"BONUS" as const,occurredAt:day,criterionId:"A",dailyLimit:2}))).finalScore).toBe(106)});it("giới hạn số lần trong tuần",()=>expect(calculateScore(100,[1,2,3].map(n=>({status:"APPROVED",points:-2,type:"PENALTY" as const,occurredAt:new Date(`2026-08-${17+n}`),criterionId:"A",weeklyLimit:2}))).finalScore).toBe(96));it("xếp loại đúng ngưỡng",()=>expect([100,90,80,65,64].map(grade)).toEqual(["Xuất sắc","Tốt","Khá","Đạt","Cần cố gắng"]));it("xếp hạng và đồng hạng",()=>expect(rankScores([{score:100},{score:90},{score:90},{score:80}]).map(x=>x.rank)).toEqual([1,2,2,4]));it("ưu tiên ít lỗi nghiêm trọng khi bằng điểm",()=>expect(rankScores([{score:90,severe:2},{score:90,severe:0}])[0].severe).toBe(0));it("sửa điểm danh không bị tính hai lần nhờ chỉ tính bản ghi đã duyệt",()=>expect(calculateScore(100,[{status:"CANCELLED",points:-2,type:"PENALTY"},{status:"APPROVED",points:-10,type:"PENALTY"}]).finalScore).toBe(90));});
