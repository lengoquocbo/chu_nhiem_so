/**
 * Cloud SaaS mode – license enforcement is handled at the platform level.
 * This is a no-op stub kept for API compatibility with existing action files.
 */
export async function assertWriteAllowed(): Promise<void> {
  // no-op in cloud mode
}